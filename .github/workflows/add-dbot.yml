name: Add Dependabot to forks

on: 
  workflow_call:
    inputs:
      repo:
        description: 'Name of the target repository'
        default: 'default'
        required: true
        type: string
  workflow_dispatch:
    inputs:
      repo:
        description: 'Name of the target repository'
        default: 'default'
        required: true
        type: string


jobs:
  add-dbot:
    name: Add Dependabot to ${{ github.event.inputs.repo }}
    runs-on: ubuntu-20.04
    steps:
      - name: Test
        run: |
          echo "${{ github.event.inputs.repo }}"